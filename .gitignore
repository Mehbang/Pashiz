node_modules/

# Docker volumes data directory
/data

# Production env file 
.env

test-results/
.qodo

# Playwright shared auth state (regenerated per run by globalSetup)
e2e/.auth/


.pnpm-store
# Pashiz deployment artefacts (created on the server by setup.sh / update.sh)
backups/
.pashiz-previous-version

# macOS Finder metadata
.DS_Store

# Uploaded import sheets. They land inside the repo, so a `git add -A` after
# someone imports a file would commit their spreadsheet.
packages/server/static/imports/*
!packages/server/static/imports/.gitkeep
